<?php

/**
 * Creates Disqus comment panes used on the site.
 */

/**
 * Implementation of hook_ctools_plugin_directory()
 */
function guq_disqus_ctools_plugin_directory($module, $plugin) {
  if ($module == 'ctools' && $plugin == 'guq_disqus') {
    return 'plugins/' . $plugin;
  }
}
